export default {
  // 文末にセミコロンを付ける(Prettierのデフォルトだが、意図を明示するために記載)
  semi: true,
  // 文字列はシングルクォートで統一
  singleQuote: true,
  // 配列・オブジェクトの末尾にもカンマを付ける(差分を最小化しやすくするため)
  trailingComma: 'all',
  // 1行の最大文字数。超えたら折り返す
  printWidth: 80,
  // インデント幅(スペース数)
  tabWidth: 2,
};
